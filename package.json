{
  "name": "lambda-image-analysis",
  "version": "1.0.0",
  "description": "",
  "main": "src/handler.mjs",
  "type": "module",
  "scripts": {
    "build": "esbuild src/handler.ts --bundle --platform=node --target=node22 --outdir=build",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [
    "aws",
    "lambda",
    "translate",
    "rekognition"
  ],
  "author": "AndrewReis",
  "license": "ISC",
  "dependencies": {
    "@aws-sdk/client-rekognition": "^3.864.0",
    "@aws-sdk/client-s3": "^3.864.0",
    "@aws-sdk/client-translate": "^3.864.0"
  },
  "devDependencies": {
    "@types/node": "^24.2.1",
    "serverless": "^4.18.0",
    "ts-node": "^10.9.2",
    "typescript": "^5.9.2",
    "vitest": "^3.2.4"
  }
}